machine:
 node:
   version: 5.5.0
deployment:
  staging:
    branch: master
    commands:
      - npm start
      - "[[ ! -s \"$(git rev-parse --git-dir)/shallow\" ]] || git fetch --unshallow"
      - git push git@heroku.com:hitfin-fake.git $CIRCLE_SHA1:refs/heads/master
